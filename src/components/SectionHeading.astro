---
interface Props {
  id?: string;
  eyebrow?: string;
  title: string;
  description?: string;
  align?: "left" | "center";
}

const { id, eyebrow, title, description, align = "left" } = Astro.props;
---

<div
  class:list={[
    "max-w-3xl",
    align === "center" && "mx-auto text-center",
  ]}
>
  {
    eyebrow && (
      <p class="font-mono text-xs uppercase tracking-[0.22em] text-accent/90">
        {eyebrow}
      </p>
    )
  }
  <h2
    id={id}
    class:list={[
      "mt-2 text-balance text-2xl font-semibold tracking-tight text-ink sm:text-3xl",
      align === "center" && "mx-auto",
    ]}
  >
    {title}
  </h2>
  {
    description && (
      <p class="mt-3 text-pretty text-base leading-relaxed text-ink-muted">
        {description}
      </p>
    )
  }
</div>
